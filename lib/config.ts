/**
 * Active practice for this deployment.
 * All API routes and the user switcher scope to this practice only.
 * Replace with a session-derived value once auth is implemented.
 */
export const PRACTICE_ID = 1; // Riverside Dental
